# v0.2.3

- Added/wired dark/light mode toggle.
- Theme preference is saved in `localStorage` under `choicebook:theme`.
- Verified debug mode remains present.

# v0.2.2

- Added missing `bartender_pity` passage referenced by three vampire bar dialogue choices.

# v0.2.1

- Fixed `story.yaml` duplicate `stats:` key that caused `js-yaml` to fail parsing.
- Repacked files with fresh timestamps.
